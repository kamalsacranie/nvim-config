return {
    -- s("selected_text", f(function(args, snip)
    --     local res, env = {}, snip.env
    --     table.insert(res,
    --         "Selected Text (current line is " .. env.TM_LINE_NUMBER .. "):")
    --     for _, ele in ipairs(env.LS_SELECT_RAW) do table.insert(res, ele) end
    --     return res
    -- end, {}))
}
